{
  "name": "sns-auto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ADHD 플래너 SNS 브랜딩 자동화 에이전트 — 반자동(승인제) 콘텐츠 파이프라인",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/**/*.test.js\"",
    "preview": "node dist/src/cli.js",
    "start": "node dist/src/cli.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
